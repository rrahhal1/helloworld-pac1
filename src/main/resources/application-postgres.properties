database=postgres
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost/student}
spring.datasource.username=${POSTGRES_USER:student}
spring.datasource.password=${POSTGRES_PASS:student}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always

